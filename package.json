{
  "name": "snap-grid-js",
  "version": "0.0.2",
  "description": "A collection of utilities.",
  "main": "public/index.html",
  "scripts": {
    "start": "live-server public --port=8080 --entry-file=public --mount=/sg:public --mount=/board:public --mount=/rws:public"
  },
  "author": "James Mulberry",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}