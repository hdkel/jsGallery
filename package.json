{
  "name": "snap-grid-js",
  "version": "0.0.2",
  "description": "A collection of utilities.",
  "main": "src/index.html",
  "scripts": {
    "start": "live-server src --port=8080 --entry-file=src --mount=/sg:src --mount=/board:src --mount=/rws:src"
  },
  "author": "James Mulberry",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}