{
  "name": "snap-grid-js",
  "version": "0.0.1",
  "description": "Gallery app that allows freely split and rearrange the viewport.",
  "main": "src/index.html",
  "scripts": {
    "start": "live-server src --port=8080 --entry-file=src --mount=/sg:src --mount=/board:src --mount=/gallery:src --mount=/rws:src"
  },
  "author": "James Mulberry",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}