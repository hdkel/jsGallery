{
  "name": "snap-grid-js",
  "version": "0.0.2",
  "description": "A collection of utilities.",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=8080 --mount=/sg:. --mount=/sg2:. --mount=/board:. --mount=/rws:. --mount=/wuwa:."
  },
  "author": "James Mulberry",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}